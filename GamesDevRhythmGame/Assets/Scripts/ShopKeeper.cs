using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShopKeeper : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }



    public void OpenShop()
    {
        //Open a menu that pauses the game and opens the shop
        //Buttons to select items with different costs
        //To buy it will check item cost against player money, if the player has enough money.
        //after purchase play sound.
        //Close shop on x click or escape key.
    }
}
